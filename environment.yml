name: novaboard-scheduling
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pip
  - requests
  - python-dotenv
  - pytest
  - pytest-cov
  - black
  - flake8
  - pandas
  - pip:
    - pydantic
    - httpx
    - "plotly[express]"
    - kaleido
